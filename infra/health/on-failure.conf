# Drop-in pro kazdou hlidanou jednotku: /etc/systemd/system/<jednotka>.d/on-failure.conf.
# Jednotky samotne (i ty z repozitaru hlidacu) se kvuli tomu nemeni; seznam je
# v infra/manifest.txt pod "health dropin". %n je jmeno jednotky, ktera selhala.
[Unit]
OnFailure=notify-failure@%n.service
